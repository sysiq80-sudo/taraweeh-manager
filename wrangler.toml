# Cloudflare Pages configuration
# Force npm install, not bun
name = "taraweeh-manager"
compatibility_date = "2024-01-15"

[build]
command = "npm run build"

[build.upload]
format = "service-worker"

[env.production]
vars = { ENVIRONMENT = "production" }

